<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tamila.cl.ejemploandroid">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".vistas.CardViewActivity"
            android:parentActivityName=".MainActivity">

            <!-- soporte para versiones anteriores a Lolypop -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity
            android:name=".vistas.PicassoActivity"
            android:parentActivityName=".MainActivity">

            <!-- soporte para versiones anteriores a Lolypop -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity
            android:name=".vistas.WebViewActivity"
            android:parentActivityName=".MainActivity">
            !-- soporte para versiones anteriores a Lolypop -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity
            android:name=".vistas.MapaActivity"
            android:parentActivityName=".MainActivity">
            !-- soporte para versiones anteriores a Lolypop -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity
            android:name=".vistas.MenuDrawerActivity"
            android:parentActivityName=".MainActivity">
            !-- soporte para versiones anteriores a Lolypop -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity
            android:name=".vistas.RetrofitActivity"
            android:parentActivityName=".MainActivity">
            !-- soporte para versiones anteriores a Lolypop -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity
            android:name=".vistas.VolleyActivity"
            android:parentActivityName=".MainActivity">
            !-- soporte para versiones anteriores a Lolypop -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".vistas.MapaGooleActivity"
            android:label="@string/title_activity_mapa_goole"
            android:parentActivityName=".MainActivity">
            !-- soporte para versiones anteriores a Lolypop -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity
            android:name=".vistas.GPSActivity"
            android:parentActivityName=".MainActivity">
            !-- soporte para versiones anteriores a Lolypop -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity
            android:name=".vistas.CardViewApiActivity"
            android:parentActivityName=".MainActivity">
            !-- soporte para versiones anteriores a Lolypop -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity
            android:name=".vistas.VolleySimpleActivity"
            android:parentActivityName=".MainActivity">
            !-- soporte para versiones anteriores a Lolypop -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity android:name=".vistas.FormularioActivity" android:parentActivityName=".MainActivity">
            !-- soporte para versiones anteriores a Lolypop -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity android:name=".vistas.AlertDialogActivity" android:parentActivityName=".MainActivity">
            !-- soporte para versiones anteriores a Lolypop -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
    </application>

</manifest>